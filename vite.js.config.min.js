import { defineConfig } from 'vite';
import { fileURLToPath, URL } from 'node:url';

export default defineConfig({
  build: {
    outDir: 'dist',
    emptyOutDir: false,
    sourcemap: true,
    minify: 'esbuild',
    lib: {
      entry: fileURLToPath(new URL('./js/vitrus.js', import.meta.url)),
      name: 'vitrus',
      formats: ['iife'],
      fileName: () => 'vitrus.bundle.min.js',
    },
    rollupOptions: {
      output: {
        exports: 'named',
      },
    },
  },
});
